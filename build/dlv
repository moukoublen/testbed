dlv \
  --listen='0.0.0.0:2345' \
  --backend="default" \
  --api-version=2 \
  --headless \
  --accept-multiclient \
  exec \
  ./output/testbed \
  --continue

# dlv \
#   --build-flags='-mod=vendor' \
#   --listen='0.0.0.0:2345' \
#   --backend="default" \
#   --api-version=2 \
#   --headless \
#   --accept-multiclient \
#   debug \
#   ./cmd/testbed
#   #--continue
#   #--log \
#   #--log-output='debugger,rpc,fncall'
